% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R-frontline.R
\name{directed_cluster_interface_pairs}
\alias{directed_cluster_interface_pairs}
\title{Build all oriented cluster pairs (A -> B) and their directed_pair labels}
\usage{
directed_cluster_interface_pairs(
  cluster_labels,
  interface_separator = "-",
  sort_clusters = FALSE
)
}
\arguments{
\item{cluster_labels}{A vector of cluster labels (character, factor, numeric, etc.).
`NA` values are ignored.}

\item{interface_separator}{Character string used to join `cluster` and
`interface_to` into the `directed_pair` label. Default is `"-"`.}

\item{sort_clusters}{Logical. If `TRUE`, unique cluster labels are sorted
(lexicographically) before building pairs. If `FALSE`, preserves first
appearance order. Default is `FALSE`.}
}
\value{
A data.frame with columns:
\describe{
  \item{cluster}{Source cluster label (character).}
  \item{interface_to}{Target cluster label (character).}
  \item{directed_pair}{Directed pair label, e.g. `"A-B"`.}
}
If fewer than 2 unique (non-NA) clusters are present, returns an empty data.frame
with the same columns.
}
\description{
Given a vector of cluster labels, this function returns all **ordered**
(oriented) pairs of distinct clusters. For each pair (`cluster`,
`interface_to`), it also creates a `directed_pair` string label such as `"A-B"`.
}
\examples{
cl <- c("1","1","2","3", NA)
directed_cluster_interface_pairs(cl)

}
