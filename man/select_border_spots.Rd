% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R-frontline.R
\name{select_border_spots}
\alias{select_border_spots}
\title{Select border spots from cluster A that touch cluster B (and flag junctions)}
\usage{
select_border_spots(
  df,
  cluster_from,
  cluster_to,
  k = 7,
  max_dist = NULL,
  coord_cols = c("x", "y"),
  cluster_col = "cluster"
)
}
\arguments{
\item{df}{A data.frame containing at least the coordinate columns and a cluster label column.}

\item{cluster_from}{Cluster label for the source cluster (A). Spots in this cluster
are tested for adjacency to `cluster_to`.}

\item{cluster_to}{Cluster label for the target cluster (B).}

\item{k}{Integer. Number of nearest neighbors to consider (excluding self).
Internally uses `k + 1` to include self then removes it. Default is 7.}

\item{max_dist}{Optional numeric. Maximum Euclidean distance for a neighbor to be
considered (distance cutoff). If `NULL`, no distance filtering is applied.}

\item{coord_cols}{Character vector of length 2 giving the coordinate column names.
Default is `c("x","y")`.}

\item{cluster_col}{Character. Name of the column containing cluster labels.
Default is `"cluster"`.}
}
\value{
A data.frame subset of `df` containing border spots from `cluster_from` to
`cluster_to` with columns: spot_id, x, y, cluster, interface_to, directed_pair,
is_border, is_border_multiple, other_adjacent_borders.
}
\description{
This function identifies **directed interface spots**: spots belonging to
`cluster_from` (A) that have at least one neighbor in `cluster_to` (B) within
a k-nearest-neighbors neighborhood (optionally constrained by a distance cutoff).
}
\details{
In addition, it flags spots that also touch **other clusters** (i.e., junction
/ multi-interface spots) and reports which other clusters are touched.


Steps:
\enumerate{
  \item Builds a kNN neighborhood for each spot in `cluster_from`.
  \item Optionally removes neighbors farther than `max_dist`.
  \item Marks a spot as a border spot if it has at least one neighbor in `cluster_to`.
  \item Flags a spot as multi-interface if it also has a neighbor belonging to a
  cluster different from `cluster_from` and `cluster_to`.
}

The returned data.frame contains only spots from `cluster_from` that touch `cluster_to`,
with columns:
\describe{
  \item{spot_id}{Spot identifier.}
  \item{x}{X coordinate.}
  \item{y}{Y coordinate.}
  \item{cluster}{Cluster label (same as `cluster_from`).}
  \item{interface_to}{The value of `cluster_to`.}
  \item{directed_pair}{Interface label combining cluster and `interface_to`, e.g. `"A-B"`.}
  \item{is_border}{Always `TRUE` for returned rows (kept for clarity).}
  \item{is_border_multiple}{`TRUE` if the spot also touches other clusters.}
  \item{other_adjacent_borders}{Comma-separated list of other clusters touched, or `NA`.}
}
}
\examples{
# Minimal example (requires RANN and dplyr)
set.seed(1)
df_ex <- data.frame(
  x = rnorm(200),
  y = rnorm(200),
  cluster = sample(c("A","B","C"), 200, replace = TRUE)
)
res <- select_border_spots(df_ex, cluster_from = "A", cluster_to = "B", k = 7)
head(res)

}
